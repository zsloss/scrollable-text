#pragma once
#include <unordered_map>
#include <vector>
#include <string>
#include <SDL.h>
#include "Utils.h"

namespace utils {
	std::vector<std::string> split(std::string input, char delimiter);
}